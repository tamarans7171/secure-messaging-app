# Example environment for local development
# Copy this file to server/.env and edit values before running

MONGO_URL=mongodb://localhost:27017/secure-chat
REDIS_URL=redis://localhost:6379
JWT_SECRET=change_me_for_demo
DEV_PERSIST_MESSAGE_KEY=1
# MESSAGE_AES_KEY can be set to a base64 32-byte value. If omitted and
# DEV_PERSIST_MESSAGE_KEY=1 the server will generate and persist one.
MESSAGE_AES_KEY=
GROUP_AES_KEY=
SSL_PFX_PATH=server/cert/server.pfx
SSL_PFX_PASS=1234
PORT=3001
NODE_ENV=development
